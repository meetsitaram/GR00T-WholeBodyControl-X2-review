# X2 heuristic planner — bin specification.
#
# Source of truth for what motion primitives the planner needs and the
# tolerance bands the curator scores against. Hand-edited.
#
# Companion files (curator-written):
#   - x2_planner_primitives.yaml            (selected clip per bin)
#   - x2_planner_primitives.pkl             (actual sliced motion data)
#   - x2_planner_primitives_report.md       (top-N candidates per bin)
#
# Distance conversions:
#     1 ft   = 0.3048 m
#     0.5 ft = 0.1524 m
#     0.25ft = 0.0762 m
#
# Tolerance bands track the design in the planner plan:
#   docs/source/references/x2_heuristic_planner.md
#
# All target_xy_m entries are body-frame [forward_m, lateral_m] (lateral is
# positive to the robot's left). All angles are in degrees. Pelvis-z band is
# in meters (root joint world z).
#
# `name_regex` is a *soft* accelerator over motion_key strings — used to
# pre-filter candidates before the metric scorer evaluates them. Leave
# unset to scan every clip in the source PKL (slower but always works).

bins:
  # =====================================================================
  # IDLE — always-on rest loop
  # =====================================================================
  - name: idle_stand
    family: idle
    target_intent: idle
    target_magnitude: default
    target_xy_m: [0.0, 0.0]
    target_yaw_deg: 0.0
    tol_xy_m: 0.04
    tol_yaw_deg: 3.0
    cross_axis_max_m: 0.04
    pelvis_z_band_m: [0.70, 0.80]
    end_at_square_min: 0.5
    feet_planted_min: 0.5
    stride_count_target: 0
    window_frames_min: 45
    window_frames_max: 150
    name_regex: "(?i)(idle|stand|rest|stationary)"

  # =====================================================================
  # CONTINUOUS WALK — distance gait (entry/exit blended on demand)
  # =====================================================================
  - name: fwd_walk_standard
    family: continuous_walk
    target_intent: walk
    target_magnitude: forward
    target_xy_m: [0.50, 0.0]
    target_yaw_deg: 0.0
    tol_xy_m: 0.20
    tol_yaw_deg: 8.0
    cross_axis_max_m: 0.10
    pelvis_z_band_m: [0.62, 0.80]
    end_at_square_min: 0.0
    stride_count_target: null
    window_frames_min: 90
    window_frames_max: 180
    name_regex: "(?i)(walk|stride|pace|gait)"

  - name: back_walk_standard
    family: continuous_walk
    target_intent: walk
    target_magnitude: backward
    target_xy_m: [-0.50, 0.0]
    target_yaw_deg: 0.0
    tol_xy_m: 0.20
    tol_yaw_deg: 8.0
    cross_axis_max_m: 0.10
    pelvis_z_band_m: [0.62, 0.80]
    end_at_square_min: 0.0
    stride_count_target: null
    window_frames_min: 90
    window_frames_max: 180
    name_regex: "(?i)(walk_backward|reverse|retreat|back_step)"

  # =====================================================================
  # FORWARD SHUFFLES — single-step, end at square stance
  # =====================================================================
  - name: fwd_step_1ft
    family: locomotion
    target_intent: fwd_step
    target_magnitude: one_ft
    target_xy_m: [0.3048, 0.0]
    target_yaw_deg: 0.0
    tol_xy_m: 0.06
    tol_yaw_deg: 4.0
    cross_axis_max_m: 0.05
    pelvis_z_band_m: [0.62, 0.80]
    end_at_square_min: 0.4
    stride_count_target: 1
    stride_count_min: 1     # require at least one real foot-lift
    window_frames_min: 30
    window_frames_max: 90
    name_regex: "(?i)(step|walk|stride|shuffle|stop)"

  - name: fwd_step_half_ft
    family: locomotion
    target_intent: fwd_step
    target_magnitude: half_ft
    target_xy_m: [0.1524, 0.0]
    target_yaw_deg: 0.0
    tol_xy_m: 0.05
    tol_yaw_deg: 4.0
    cross_axis_max_m: 0.05
    pelvis_z_band_m: [0.62, 0.80]
    end_at_square_min: 0.4
    stride_count_target: 1
    stride_count_min: 1     # was selecting body-sway clips with strides=0
    window_frames_min: 30
    window_frames_max: 90
    name_regex: "(?i)(step|walk|stride|shuffle|approach)"

  - name: fwd_step_quarter_ft
    family: locomotion
    target_intent: fwd_step
    target_magnitude: quarter_ft
    target_xy_m: [0.0762, 0.0]
    target_yaw_deg: 0.0
    tol_xy_m: 0.04
    tol_yaw_deg: 4.0
    cross_axis_max_m: 0.04
    pelvis_z_band_m: [0.62, 0.80]
    end_at_square_min: 0.4
    stride_count_target: 1
    stride_count_min: 1
    window_frames_min: 25
    window_frames_max: 75
    name_regex: "(?i)(step|walk|stride|shuffle|nudge)"

  # =====================================================================
  # BACKWARD SHUFFLES
  # =====================================================================
  - name: back_step_half_ft
    family: locomotion
    target_intent: back_step
    target_magnitude: half_ft
    target_xy_m: [-0.1524, 0.0]
    target_yaw_deg: 0.0
    tol_xy_m: 0.05
    tol_yaw_deg: 4.0
    cross_axis_max_m: 0.05
    pelvis_z_band_m: [0.62, 0.80]
    end_at_square_min: 0.4
    stride_count_target: 1
    stride_count_min: 1
    window_frames_min: 30
    window_frames_max: 90
    name_regex: "(?i)(back|reverse|retreat|step|shuffle)"

  - name: back_step_quarter_ft
    family: locomotion
    target_intent: back_step
    target_magnitude: quarter_ft
    target_xy_m: [-0.0762, 0.0]
    target_yaw_deg: 0.0
    tol_xy_m: 0.04
    tol_yaw_deg: 4.0
    cross_axis_max_m: 0.04
    pelvis_z_band_m: [0.62, 0.80]
    end_at_square_min: 0.4
    stride_count_target: 1
    stride_count_min: 1     # was selecting sway clips with strides=0
    window_frames_min: 25
    window_frames_max: 75
    name_regex: "(?i)(back|reverse|step|shuffle)"

  # =====================================================================
  # LATERAL SHUFFLES
  # =====================================================================
  # Single canonical side-step per direction. The mocap source
  # (walk_sideway_045_stop_001__A038_M) is a back-right diagonal at
  # ~45 deg of ~37 cm total (28 cm back + 25 cm right). 0.5x scaled
  # variants were untrackable by the policy; 2x scaled variants
  # exceed the policy's tracking envelope. Any magnitude token from
  # the (side_left|side_right) intent collapses to this single bin.
  - name: side_left_step
    family: locomotion
    # Derived from side_right_step via mirror_lr; both bins are
    # the user-approved A038_M [0, 90] window (see recipes YAML's
    # source_ref.approved_bake_pkl for the original SONIC playback
    # the user blessed on 2026-05-11).
    target_intent: side_left
    target_magnitude: default
    target_xy_m: [-0.25, 0.27]
    target_yaw_deg: 0.0
    tol_xy_m: 0.10
    tol_yaw_deg: 8.0
    cross_axis_max_m: 0.10
    pelvis_z_band_m: [0.62, 0.80]
    end_at_square_min: 0.4
    stride_count_target: 1
    stride_count_min: 1
    window_frames_min: 60
    window_frames_max: 120
    name_regex: "(?i)(side|sidestep|lateral|shuffle|crab|left)"

  - name: side_right_step
    family: locomotion
    # User-approved A038_M [0, 90] window (see recipes YAML's
    # source_ref.approved_bake_pkl for the original SONIC playback
    # the user blessed on 2026-05-11).
    target_intent: side_right
    target_magnitude: default
    target_xy_m: [-0.25, -0.27]
    target_yaw_deg: 0.0
    tol_xy_m: 0.10
    tol_yaw_deg: 8.0
    cross_axis_max_m: 0.10
    pelvis_z_band_m: [0.62, 0.80]
    end_at_square_min: 0.4
    stride_count_target: 1
    stride_count_min: 1
    window_frames_min: 60
    window_frames_max: 120
    name_regex: "(?i)(side|sidestep|lateral|shuffle|crab|right)"

  # =====================================================================
  # IN-PLACE TURNS — pivot left
  # =====================================================================
  - name: turn_left_15deg
    family: locomotion
    target_intent: turn_left
    target_magnitude: deg_15
    target_xy_m: [0.0, 0.0]
    target_yaw_deg: 15.0
    tol_xy_m: 0.05
    tol_yaw_deg: 3.0
    cross_axis_max_m: 0.05
    pelvis_z_band_m: [0.62, 0.80]
    end_at_square_min: 0.4
    stride_count_target: null
    window_frames_min: 30
    window_frames_max: 75
    name_regex: "(?i)(turn|pivot|rotate|spin|left|cw|ccw)"

  - name: turn_left_30deg
    family: locomotion
    target_intent: turn_left
    target_magnitude: deg_30
    target_xy_m: [0.0, 0.0]
    target_yaw_deg: 30.0
    tol_xy_m: 0.06
    tol_yaw_deg: 4.0
    cross_axis_max_m: 0.06
    pelvis_z_band_m: [0.62, 0.80]
    end_at_square_min: 0.4
    stride_count_target: null
    window_frames_min: 30
    window_frames_max: 90
    name_regex: "(?i)(turn|pivot|rotate|spin|left|ccw)"

  - name: turn_left_45deg
    family: locomotion
    target_intent: turn_left
    target_magnitude: deg_45
    target_xy_m: [0.0, 0.0]
    target_yaw_deg: 45.0
    tol_xy_m: 0.08
    tol_yaw_deg: 5.0
    cross_axis_max_m: 0.08
    pelvis_z_band_m: [0.62, 0.80]
    end_at_square_min: 0.4
    stride_count_target: null
    window_frames_min: 45
    window_frames_max: 105
    name_regex: "(?i)(turn|pivot|rotate|spin|left|ccw)"

  - name: turn_left_90deg
    family: locomotion
    target_intent: turn_left
    target_magnitude: deg_90
    target_xy_m: [0.0, 0.0]
    target_yaw_deg: 90.0
    tol_xy_m: 0.10
    tol_yaw_deg: 8.0
    cross_axis_max_m: 0.10
    pelvis_z_band_m: [0.62, 0.80]
    end_at_square_min: 0.4
    stride_count_target: null
    window_frames_min: 60
    window_frames_max: 150
    name_regex: "(?i)(turn|pivot|rotate|spin|left|ccw)"

  # =====================================================================
  # IN-PLACE TURNS — pivot right
  # =====================================================================
  - name: turn_right_15deg
    family: locomotion
    target_intent: turn_right
    target_magnitude: deg_15
    target_xy_m: [0.0, 0.0]
    target_yaw_deg: -15.0
    tol_xy_m: 0.05
    tol_yaw_deg: 3.0
    cross_axis_max_m: 0.05
    pelvis_z_band_m: [0.62, 0.80]
    end_at_square_min: 0.4
    stride_count_target: null
    window_frames_min: 30
    window_frames_max: 75
    name_regex: "(?i)(turn|pivot|rotate|spin|right|cw)"

  - name: turn_right_30deg
    family: locomotion
    target_intent: turn_right
    target_magnitude: deg_30
    target_xy_m: [0.0, 0.0]
    target_yaw_deg: -30.0
    tol_xy_m: 0.06
    tol_yaw_deg: 4.0
    cross_axis_max_m: 0.06
    pelvis_z_band_m: [0.62, 0.80]
    end_at_square_min: 0.4
    stride_count_target: null
    window_frames_min: 30
    window_frames_max: 90
    name_regex: "(?i)(turn|pivot|rotate|spin|right|cw)"

  - name: turn_right_45deg
    family: locomotion
    target_intent: turn_right
    target_magnitude: deg_45
    target_xy_m: [0.0, 0.0]
    target_yaw_deg: -45.0
    tol_xy_m: 0.08
    tol_yaw_deg: 5.0
    cross_axis_max_m: 0.08
    pelvis_z_band_m: [0.62, 0.80]
    end_at_square_min: 0.4
    stride_count_target: null
    window_frames_min: 45
    window_frames_max: 105
    name_regex: "(?i)(turn|pivot|rotate|spin|right|cw)"

  - name: turn_right_90deg
    family: locomotion
    target_intent: turn_right
    target_magnitude: deg_90
    target_xy_m: [0.0, 0.0]
    target_yaw_deg: -90.0
    tol_xy_m: 0.10
    tol_yaw_deg: 8.0
    cross_axis_max_m: 0.10
    pelvis_z_band_m: [0.62, 0.80]
    end_at_square_min: 0.4
    stride_count_target: null
    window_frames_min: 60
    window_frames_max: 150
    name_regex: "(?i)(turn|pivot|rotate|spin|right|cw)"

  # =====================================================================
  # STATIC UPPER-BODY — feet planted, end-at-apex (held)
  #
  # All bins below set ``freeze_arms_to_default: true``. The curated PKL is
  # written with arm + head DOFs forced to DEFAULT_STAND_POSE_MUJOCO_RAD so
  # the planner only commands waist/torso. Arms are owned downstream by the
  # VLA / teleop source (mock_vla_publish_stand_token / Quest3 IK / etc.).
  # =====================================================================
  # Forward leans: synth in v6 (waist_pitch + 0.30 hip_pitch_share). The
  # peak waist angle is HARD-CAPPED at 20 deg inside
  # op_synthesize_waist_ramp; magnitudes here mirror the cap (8 / 14 / 20).
  - name: lean_fwd_small
    family: static_upper_body
    target_intent: lean_fwd
    target_magnitude: small
    target_waist_pitch_deg: 8.0
    tol_waist_deg: 3.0
    pelvis_z_band_m: [0.70, 0.80]
    end_at_apex_min: 0.7
    feet_planted_min: 0.4
    stride_count_target: 0
    freeze_arms_to_default: true
    window_frames_min: 30
    window_frames_max: 90
    name_regex: "(?i)(lean|bend|reach|forward|pickup|pick)"

  - name: lean_fwd_medium
    family: static_upper_body
    target_intent: lean_fwd
    target_magnitude: medium
    target_waist_pitch_deg: 14.0
    tol_waist_deg: 5.0
    pelvis_z_band_m: [0.68, 0.80]
    end_at_apex_min: 0.7
    feet_planted_min: 0.4
    stride_count_target: 0
    freeze_arms_to_default: true
    window_frames_min: 30
    window_frames_max: 90
    name_regex: "(?i)(lean|bend|reach|forward|pickup|pick|stoop)"

  - name: lean_fwd_large
    family: static_upper_body
    target_intent: lean_fwd
    target_magnitude: large
    target_waist_pitch_deg: 20.0
    tol_waist_deg: 6.0
    pelvis_z_band_m: [0.66, 0.80]
    end_at_apex_min: 0.7
    feet_planted_min: 0.4
    stride_count_target: 0
    freeze_arms_to_default: true
    window_frames_min: 30
    window_frames_max: 90
    name_regex: "(?i)(lean|bend|reach|forward|pickup|stoop|squat|crouch)"

  # Lateral leans (NEW in v6): pure waist_roll synthesis, hard-capped at
  # 10 deg inside op_synthesize_waist_ramp. Lateral support polygon on
  # the X2 is much tighter than sagittal (foot half-width ~5 cm vs
  # half-length ~12 cm), so magnitudes here are smaller (4 / 7 / 10).
  - name: lean_left_small
    family: static_upper_body
    target_intent: lean_left
    target_magnitude: small
    target_waist_roll_deg: 4.0
    tol_waist_deg: 2.0
    pelvis_z_band_m: [0.70, 0.80]
    end_at_apex_min: 0.7
    feet_planted_min: 0.4
    stride_count_target: 0
    freeze_arms_to_default: true
    window_frames_min: 30
    window_frames_max: 90
    name_regex: "(?i)(lean|tilt|side|lateral|reach)"

  - name: lean_left_medium
    family: static_upper_body
    target_intent: lean_left
    target_magnitude: medium
    target_waist_roll_deg: 7.0
    tol_waist_deg: 3.0
    pelvis_z_band_m: [0.70, 0.80]
    end_at_apex_min: 0.7
    feet_planted_min: 0.4
    stride_count_target: 0
    freeze_arms_to_default: true
    window_frames_min: 30
    window_frames_max: 90
    name_regex: "(?i)(lean|tilt|side|lateral|reach)"

  - name: lean_left_large
    family: static_upper_body
    target_intent: lean_left
    target_magnitude: large
    target_waist_roll_deg: 10.0
    tol_waist_deg: 3.0
    pelvis_z_band_m: [0.70, 0.80]
    end_at_apex_min: 0.7
    feet_planted_min: 0.4
    stride_count_target: 0
    freeze_arms_to_default: true
    window_frames_min: 30
    window_frames_max: 90
    name_regex: "(?i)(lean|tilt|side|lateral|reach)"

  - name: lean_right_small
    family: static_upper_body
    target_intent: lean_right
    target_magnitude: small
    target_waist_roll_deg: -4.0
    tol_waist_deg: 2.0
    pelvis_z_band_m: [0.70, 0.80]
    end_at_apex_min: 0.7
    feet_planted_min: 0.4
    stride_count_target: 0
    freeze_arms_to_default: true
    window_frames_min: 30
    window_frames_max: 90
    name_regex: "(?i)(lean|tilt|side|lateral|reach)"

  - name: lean_right_medium
    family: static_upper_body
    target_intent: lean_right
    target_magnitude: medium
    target_waist_roll_deg: -7.0
    tol_waist_deg: 3.0
    pelvis_z_band_m: [0.70, 0.80]
    end_at_apex_min: 0.7
    feet_planted_min: 0.4
    stride_count_target: 0
    freeze_arms_to_default: true
    window_frames_min: 30
    window_frames_max: 90
    name_regex: "(?i)(lean|tilt|side|lateral|reach)"

  - name: lean_right_large
    family: static_upper_body
    target_intent: lean_right
    target_magnitude: large
    target_waist_roll_deg: -10.0
    tol_waist_deg: 3.0
    pelvis_z_band_m: [0.70, 0.80]
    end_at_apex_min: 0.7
    feet_planted_min: 0.4
    stride_count_target: 0
    freeze_arms_to_default: true
    window_frames_min: 30
    window_frames_max: 90
    name_regex: "(?i)(lean|tilt|side|lateral|reach)"

  # =====================================================================
  # CROUCH — feet planted, pelvis Z drops below standing baseline. Used
  # to lower CG for stability or to reach low-shelf objects without bending
  # forward. NEW in v2: source clips found via mine_x2_motion_candidates.
  # =====================================================================
  - name: crouch_small
    family: static_upper_body
    target_intent: crouch
    target_magnitude: small
    target_waist_pitch_deg: 0.0
    tol_waist_deg: 8.0
    pelvis_z_band_m: [0.60, 0.80]
    end_at_apex_min: 0.5
    feet_planted_min: 0.4
    stride_count_target: 0
    freeze_arms_to_default: true
    window_frames_min: 30
    window_frames_max: 90
    name_regex: "(?i)(crouch|squat|reach_low|low|kneel|stoop)"

  - name: crouch_medium
    family: static_upper_body
    target_intent: crouch
    target_magnitude: medium
    target_waist_pitch_deg: 0.0
    tol_waist_deg: 10.0
    pelvis_z_band_m: [0.55, 0.80]
    end_at_apex_min: 0.5
    feet_planted_min: 0.4
    stride_count_target: 0
    freeze_arms_to_default: true
    window_frames_min: 30
    window_frames_max: 90
    name_regex: "(?i)(crouch|squat|reach_low|low|kneel|stoop)"

  - name: crouch_large
    family: static_upper_body
    target_intent: crouch
    target_magnitude: large
    target_waist_pitch_deg: 0.0
    tol_waist_deg: 12.0
    pelvis_z_band_m: [0.50, 0.80]
    end_at_apex_min: 0.5
    feet_planted_min: 0.4
    stride_count_target: 0
    freeze_arms_to_default: true
    window_frames_min: 30
    window_frames_max: 90
    name_regex: "(?i)(crouch|squat|reach_low|low|kneel|stoop)"

  - name: torso_left_15deg
    family: static_upper_body
    target_intent: torso_left
    target_magnitude: deg_15
    target_waist_yaw_deg: 15.0
    tol_waist_deg: 5.0
    pelvis_z_band_m: [0.70, 0.80]
    end_at_apex_min: 0.7
    feet_planted_min: 0.4
    stride_count_target: 0
    freeze_arms_to_default: true
    window_frames_min: 30
    window_frames_max: 75
    name_regex: "(?i)(torso|twist|rotate|waist|left|reach)"

  - name: torso_left_30deg
    family: static_upper_body
    target_intent: torso_left
    target_magnitude: deg_30
    target_waist_yaw_deg: 30.0
    tol_waist_deg: 6.0
    pelvis_z_band_m: [0.70, 0.80]
    end_at_apex_min: 0.7
    feet_planted_min: 0.4
    stride_count_target: 0
    freeze_arms_to_default: true
    window_frames_min: 30
    window_frames_max: 90
    name_regex: "(?i)(torso|twist|rotate|waist|left|reach)"

  - name: torso_left_40deg
    family: static_upper_body
    target_intent: torso_left
    target_magnitude: deg_40
    target_waist_yaw_deg: 40.0
    tol_waist_deg: 6.0
    pelvis_z_band_m: [0.70, 0.80]
    end_at_apex_min: 0.7
    feet_planted_min: 0.4
    stride_count_target: 0
    freeze_arms_to_default: true
    window_frames_min: 30
    window_frames_max: 90
    name_regex: "(?i)(torso|twist|rotate|waist|left|reach)"

  - name: torso_right_15deg
    family: static_upper_body
    target_intent: torso_right
    target_magnitude: deg_15
    target_waist_yaw_deg: -15.0
    tol_waist_deg: 5.0
    pelvis_z_band_m: [0.70, 0.80]
    end_at_apex_min: 0.7
    feet_planted_min: 0.4
    stride_count_target: 0
    freeze_arms_to_default: true
    window_frames_min: 30
    window_frames_max: 75
    name_regex: "(?i)(torso|twist|rotate|waist|right|reach)"

  - name: torso_right_30deg
    family: static_upper_body
    target_intent: torso_right
    target_magnitude: deg_30
    target_waist_yaw_deg: -30.0
    tol_waist_deg: 6.0
    pelvis_z_band_m: [0.70, 0.80]
    end_at_apex_min: 0.7
    feet_planted_min: 0.4
    stride_count_target: 0
    freeze_arms_to_default: true
    window_frames_min: 30
    window_frames_max: 90
    name_regex: "(?i)(torso|twist|rotate|waist|right|reach)"

  - name: torso_right_40deg
    family: static_upper_body
    target_intent: torso_right
    target_magnitude: deg_40
    target_waist_yaw_deg: -40.0
    tol_waist_deg: 6.0
    pelvis_z_band_m: [0.70, 0.80]
    end_at_apex_min: 0.7
    feet_planted_min: 0.4
    stride_count_target: 0
    freeze_arms_to_default: true
    window_frames_min: 30
    window_frames_max: 90
    name_regex: "(?i)(torso|twist|rotate|waist|right|reach)"
